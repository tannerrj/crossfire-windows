diff --git a/socket/loop.cpp b/socket/loop.cpp
index c79852a73..2061b4728 100644
--- a/socket/loop.cpp
+++ b/socket/loop.cpp
@@ -614,13 +614,17 @@ void do_server(void) {
                 check_all_fds();
             }
             LOG(llevError, "select failed: %s\n", strerror(errno));
-            
-            #ifdef WIN32
-            int winsockerror = WSAGetLastError();
-            LOG(llevError, "Winsock error was: %d\n", winsockerror);
-            #endif
-            
+#ifdef WIN32
+            int wsa_err = WSAGetLastError();
+            if (wsa_err == 10004) { return; } /* WSAEINTR */
+            if (wsa_err == 10038) { check_all_fds(); return; } /* WSAENOTSOCK */
+            if (wsa_err == 10022) { check_all_fds(); return; } /* WSAEINVAL */
+            LOG(llevError, "select failed: Winsock error %d\n", wsa_err);
             return;
+#else
+            LOG(llevError, "select failed: %s\n", strerror(errno));
+            return;
+#endif
         } else if (!pollret) {
             return;
         }
